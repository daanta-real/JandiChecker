
/*    @Test
    public void init() throws Exception {

        // 초기화(환경변수 로드)
        log.debug("초기화 시작..");
        Initializer.ready();
        log.debug("변수 로딩 완료.");

        // API 테스트 시작
        log.debug("테스트 시작.");
        log.debug("Google translationTest API 키: {}", Initializer.getToken_googleTranslateAPI());
        log.debug("Google translationTest API 주소: {}", trAPIURL);

        // API REQUEST
        log.debug("API를 요청합니다..");
        URL url = new URL(trAPIURL);
        HttpURLConnection conn = (HttpURLConnection)url.openConnection();
        String result = CommonUtils.httpRequest(conn);
    }*/